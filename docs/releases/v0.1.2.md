# Codex Scope v0.1.2

Release-readiness and self-reporting correctness patch for the public V0.1 line.

## Fixed

- CLI `--version` and JSON `compatibility.codexScopeVersion` now derive from the same `package.json` version source, preventing self-reported version drift.

## Launch-readiness improvements included since v0.1.1

- Reframed the README around a fast problem/proof/quickstart flow and a factual comparison with native Codex diagnostics.
- Reordered the roadmap around real-world conformance and compatibility evidence rather than volatile Hooks semantics.
- Added a current-upstream AGENTS/CODEX_HOME parity fixture, safer issue templates, a deterministic demo script, and post-v0.1.1 strategy evidence.

## Scope

This patch does not add a V0.2 runtime feature and does not broaden the supported Codex semantics surface.

## Package

npm package: `codex-scope-inspector@0.1.2`

CLI command: `codex-scope`

License: Apache-2.0
