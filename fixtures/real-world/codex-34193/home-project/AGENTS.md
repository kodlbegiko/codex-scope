# CANONICAL_DEDUPE_MARKER

This deterministic fixture represents the current observed upstream behavior where one physical AGENTS.md can contribute as both user/global and project instructions when CODEX_HOME is also the project root.
