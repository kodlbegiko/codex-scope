# Security

Codex Scope inspects local developer configuration and may encounter credentials.

Please report vulnerabilities privately through GitHub's security-reporting mechanism when available. Do not include real secrets in public issues or fixtures.

V0.1 redacts secret-like configuration keys by default, but the heuristic is not a proof that every possible secret name will be detected.
